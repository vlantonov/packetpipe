apiVersion: 1

providers:
  - name: PacketPipe
    orgId: 1
    folder: PacketPipe
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards
